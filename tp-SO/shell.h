int parametersNumber;// Is the buffer argument numbers, represents the columns.
typedef struct cell
{
    char *parameter;
    //Cell *prox;
}Cell;

typedef struct ListCell
{
    //Cell *first;
}list;

void analiseLine();
void executeLine();
